import { writable } from 'svelte/store';

export const user_header_action_open = writable<boolean>(false)
export const isSideNavOpen = writable<boolean>(false);
export const showA2HS = writable<boolean>(false);
